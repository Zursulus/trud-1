[Service]
# The web service uses ProtectSystem=strict, while Django stores runtime uploads
# under /opt/trud-1-site/private-data. Allow only that private upload directory
# to be writable inside the service mount namespace.
ReadWritePaths=/opt/trud-1-site/private-data
